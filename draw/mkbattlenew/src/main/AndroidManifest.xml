<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="com.drawfanatteberad.mortalfived.DrawLessonsApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.D3rawletsGo">

        <activity
            android:name="com.drawfanatteberad.mortalfived.activities.SplashscreenActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.D3rawletsGo.NoActionBar"
            tools:ignore="LockedOrientationActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.drawfanatteberad.mortalfived.ui.activities.ActivityMain"
            android:theme="@style/Theme.D3rawletsGo.NoActionBar" />

        <activity
            android:name="com.drawfanatteberad.mortalfived.activities.ThemeTitleActivity"
            android:theme="@style/Theme.D3rawletsGo.NoActionBar" />

        <activity
            android:name="com.drawfanatteberad.mortalfived.activities.DrawLessonStepsActivity"
            android:theme="@style/Theme.D3rawletsGo.NoActionBar" />

        <!-- [START firebase_service] -->
        <service
            android:name="com.drawfanatteberad.mortalfived.services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service> <!-- [END firebase_service] -->

        <!-- firebase -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/melkaya" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/blue" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/prosto_notification_channel_id" />


        <!-- Delay app measurement until MobileAds.initialize() is called. -->
        <meta-data
            android:name="com.google.android.gms.ads.DELAY_APP_MEASUREMENT_INIT"
            android:value="true" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/app_id" />
    </application>

</manifest>